process {
    executor = 'awsbatch'
    queueSize = 100      
    errorStrategy = 'retry'       
    maxRetries = 2  
    maxErrors = 10
}
process.resourceLabels = ['allen-batch-pipeline': 'Truncated Multiplane-ophys-pipeline']